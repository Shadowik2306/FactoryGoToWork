@using FactoryContracts.ViewModels

@model List<ReinforcedViewModel>

@{
	ViewData["Title"] = "Home Page";
}

<div class="text-center">
	<h1 class="display-4">Изделия</h1>
</div>


<div class="text-center">
	@{
		if (Model == null)
		{
					<h3 class="display-4">Авторизируйтесь</h3>
			return;
		}

			<p>
				<a asp-action="Create">Создать изделие</a>
			</p>
			<table class="table">
				<thead>
					<tr>
						<th>
							Номер
						</th>
						<th>
							Инженер
						</th>
						<th>
							Название
						</th>
					</tr>
				</thead>
				<tbody>
				@foreach (var item in Model)
				{
							<tr>
								<td>
							@Html.DisplayFor(modelItem => item.Id)
								</td>
								<td>
							@Html.DisplayFor(modelItem => item.EngenierId)
								</td>
								<td>
							@Html.DisplayFor(modelItem => item.ReinforcedName)
								</td>
							</tr>
				}
				</tbody>
			</table>
	}
</div>